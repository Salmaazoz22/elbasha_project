# Build output
dist/
dist-drafts/

# Unused original files kept locally only (client decision 2026-09-15).
# Still available in git history at tag original-upload.
archive/

# Raw client media (WhatsApp/Facebook) and working copies — never committed.
/incoming-photos&videos/
/media-work/

# Dependencies (the build has none; media tooling installs sharp on demand)
node_modules/
package-lock.json

# Environment files (never commit real values)
.env
.env.*
!.env.example

# OS / editor
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.log
